import React from "react";

const Loader = () => {
	return (
		<span className="loading loading-bars loading-lg backdrop-blur-sm absolute inset-0 flex items-center justify-center"></span>
	);
};

export default Loader;
